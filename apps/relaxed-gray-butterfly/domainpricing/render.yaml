# relaxed-gray-butterfly/domainpricing — generated by the Airo factory.
# Prompt: let's make a site advertising domain pricing Build this as a simple single-page static site: - One self-contained index.html with all CSS and JS inline. - No ba

services:
  - type: web
    name: airo-relaxed-gray-domainpricing-web
    runtime: static
    rootDir: apps/relaxed-gray-butterfly/domainpricing
    buildCommand: true
    staticPublishPath: .
    autoDeployTrigger: commit
    buildFilter:
      paths:
        - apps/relaxed-gray-butterfly/domainpricing/**