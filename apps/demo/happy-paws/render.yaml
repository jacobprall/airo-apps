# demo/happy-paws — generated by the Airo factory.
# Prompt: Build a landing page for a dog walking business called Happy Paws

services:
  - type: web
    name: airo-demo-happy-paws-web
    runtime: static
    plan: starter
    region: oregon
    rootDir: apps/demo/happy-paws/.
    buildCommand: echo 'Static site — no build step required'
    staticPublishPath: .
    autoDeployTrigger: commit
    buildFilter:
      paths:
        - apps/demo/happy-paws/./**