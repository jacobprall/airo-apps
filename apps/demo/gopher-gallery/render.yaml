# demo/gopher-gallery — generated by the Airo factory.
# Prompt: a static site for selling gophers

services:
  - type: web
    name: airo-demo-gopher-gallery-web
    runtime: static
    rootDir: apps/demo/gopher-gallery
    buildCommand: echo 'no build needed'
    staticPublishPath: .
    autoDeployTrigger: commit
    buildFilter:
      paths:
        - apps/demo/gopher-gallery/**